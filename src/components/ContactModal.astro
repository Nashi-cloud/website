<div
    x-data="{ 
        open: false, 
        step: 'form', // form, sending, success, error
        formData: {
            name: '',
            email: '',
            company: '',
            offer: '',
            message: ''
        },
        errorMessage: '',
        
        init() {
            window.addEventListener('open-contact-modal', (e) => {
                this.open = true;
                this.step = 'form';
                if (e.detail && e.detail.offer) {
                    this.formData.offer = e.detail.offer;
                }
            });
        },

        async submitForm() {
            this.step = 'sending';
            
            try {
                const response = await fetch('https://auto2.raphaeldev.fr/webhook/2bc9b260-94dc-4454-be02-83697768ec38', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json',
                    },
                    body: JSON.stringify(this.formData)
                });

                if (response.ok) {
                    this.step = 'success';
                    this.formData = { name: '', email: '', company: '', offer: '', message: '' }; // Reset
                } else {
                    throw new Error('Erreur serveur');
                }
            } catch (error) {
                console.error('Erreur envoi formulaire:', error);
                this.step = 'error';
                this.errorMessage = 'Une erreur technique est survenue.';
            }
        }
    }"
    x-show="open"
    x-transition:enter="transition ease-out duration-300"
    x-transition:enter-start="opacity-0"
    x-transition:enter-end="opacity-100"
    x-transition:leave="transition ease-in duration-200"
    x-transition:leave-start="opacity-100"
    x-transition:leave-end="opacity-0"
    class="fixed inset-0 z-50 overflow-y-auto"
    style="display: none;"
    aria-labelledby="modal-title" 
    role="dialog" 
    aria-modal="true"
>
    <!-- Backdrop -->
    <div class="flex items-end justify-center min-h-screen pt-4 px-4 pb-20 text-center sm:block sm:p-0">
        <div 
            x-show="open"
            x-transition:enter="transition ease-out duration-300"
            x-transition:enter-start="opacity-0"
            x-transition:enter-end="opacity-100"
            x-transition:leave="transition ease-in duration-200"
            x-transition:leave-start="opacity-100"
            x-transition:leave-end="opacity-0"
            class="fixed inset-0 bg-nashi-900/75 transition-opacity" 
            @click="open = false"
            aria-hidden="true"
        ></div>

        <span class="hidden sm:inline-block sm:align-middle sm:h-screen" aria-hidden="true">&#8203;</span>

        <!-- Modal Panel -->
        <div 
            x-show="open"
            x-transition:enter="transition ease-out duration-300"
            x-transition:enter-start="opacity-0 translate-y-4 sm:translate-y-0 sm:scale-95"
            x-transition:enter-end="opacity-100 translate-y-0 sm:scale-100"
            x-transition:leave="transition ease-in duration-200"
            x-transition:leave-start="opacity-100 translate-y-0 sm:scale-100"
            x-transition:leave-end="opacity-0 translate-y-4 sm:translate-y-0 sm:scale-95"
            class="inline-block align-bottom bg-white rounded-2xl text-left overflow-hidden shadow-xl transform transition-all sm:my-8 sm:align-middle sm:max-w-lg w-full"
        >
            <div class="bg-white px-4 pt-5 pb-4 sm:p-6 sm:pb-4">
                
                <!-- Header -->
                <div class="flex justify-between items-start mb-5">
                    <h3 class="text-2xl font-bold text-nashi-900" id="modal-title">
                        <span x-show="step === 'form' || step === 'sending'">Contactez-nous</span>
                        <span x-show="step === 'success'">Message envoyé ! 🚀</span>
                        <span x-show="step === 'error'">Oups...</span>
                    </h3>
                    <button @click="open = false" class="text-gray-400 hover:text-gray-500 transition-colors">
                        <span class="sr-only">Fermer</span>
                        <svg class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
                        </svg>
                    </button>
                </div>

                <!-- Content -->
                
                <!-- STEP: FORM -->
                <form x-show="step === 'form'" @submit.prevent="submitForm" class="space-y-4">
                    <div>
                        <label for="offer" class="block text-sm font-medium text-nashi-900">Offre concernée</label>
                        <select id="offer" x-model="formData.offer" class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-nashi-500 focus:ring-nashi-500 sm:text-sm py-2 px-3 border">
                            <option value="">Je ne sais pas encore</option>
                            
                            <optgroup label="Cloud Souverain (Nextcloud)">
                                <option value="Starter">Starter (5 utilisateurs)</option>
                                <option value="Pro">Pro (15 utilisateurs)</option>
                                <option value="Enterprise">Enterprise (30+ utilisateurs)</option>
                            </optgroup>

                            <optgroup label="Intelligence Artificielle">
                                <option value="Intelligence Starter">IA Starter</option>
                                <option value="Intelligence Pro">IA Pro</option>
                                <option value="Intelligence Enterprise">IA Enterprise</option>
                            </optgroup>

                            <optgroup label="Automatisation (n8n)">
                                <option value="n8n Starter">n8n Starter</option>
                                <option value="n8n Pro">n8n Pro</option>
                                <option value="n8n Enterprise">n8n Enterprise</option>
                            </optgroup>

                            <optgroup label="Développement Python">
                                <option value="Python Runner Start">Runner Start</option>
                                <option value="Python Runner Pro">Runner Pro</option>
                                <option value="Python Runner Elite">Runner Elite</option>
                            </optgroup>

                            <option value="Sur Mesure">Autre / Sur Mesure</option>
                        </select>
                    </div>

                    <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
                        <div>
                            <label for="name" class="block text-sm font-medium text-nashi-900">Nom complet *</label>
                            <input type="text" id="name" required x-model="formData.name" class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-nashi-500 focus:ring-nashi-500 sm:text-sm py-2 px-3 border">
                        </div>
                        <div>
                            <label for="company" class="block text-sm font-medium text-nashi-900">Société</label>
                            <input type="text" id="company" x-model="formData.company" class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-nashi-500 focus:ring-nashi-500 sm:text-sm py-2 px-3 border">
                        </div>
                    </div>

                    <div>
                        <label for="email" class="block text-sm font-medium text-nashi-900">Email professionnel *</label>
                        <input type="email" id="email" required x-model="formData.email" class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-nashi-500 focus:ring-nashi-500 sm:text-sm py-2 px-3 border">
                    </div>

                    <div>
                        <label for="message" class="block text-sm font-medium text-nashi-900">Message (Optionnel)</label>
                        <textarea id="message" rows="3" x-model="formData.message" class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-nashi-500 focus:ring-nashi-500 sm:text-sm py-2 px-3 border"></textarea>
                    </div>

                    <div class="pt-2">
                        <button type="submit" class="w-full inline-flex justify-center rounded-full border border-transparent shadow-sm px-4 py-3 bg-nashi-600 text-base font-medium text-white hover:bg-nashi-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-nashi-500 sm:text-sm transition-colors">
                            Envoyer ma demande
                        </button>
                    </div>
                    <p class="text-xs text-gray-500 text-center">Vos données restent confidentielles et ne sont jamais revendues.</p>
                </form>

                <!-- STEP: SENDING -->
                <div x-show="step === 'sending'" class="py-12 text-center">
                    <svg class="animate-spin h-10 w-10 text-nashi-600 mx-auto mb-4" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24">
                        <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
                        <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
                    </svg>
                    <p class="text-nashi-900 font-medium">Envoi en cours...</p>
                </div>

                <!-- STEP: SUCCESS -->
                <div x-show="step === 'success'" class="py-6 text-center space-y-4">
                    <div class="mx-auto flex items-center justify-center h-16 w-16 rounded-full bg-green-100">
                        <svg class="h-8 w-8 text-green-600" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
                        </svg>
                    </div>
                    <p class="text-gray-600">
                        Merci ! Nous avons bien reçu votre demande.<br>
                        Notre équipe technique vous recontactera sous 24h ouvrées.
                    </p>
                    <button @click="open = false" class="mt-4 w-full inline-flex justify-center rounded-full border border-transparent shadow-sm px-4 py-2 bg-nashi-100 text-base font-medium text-nashi-900 hover:bg-nashi-200 focus:outline-none sm:text-sm">
                        Fermer
                    </button>
                </div>

                <!-- STEP: ERROR -->
                <div x-show="step === 'error'" class="py-6 text-center space-y-4">
                    <div class="mx-auto flex items-center justify-center h-16 w-16 rounded-full bg-red-100">
                        <svg class="h-8 w-8 text-red-600" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z" />
                        </svg>
                    </div>
                    <p class="text-gray-600">
                        Une erreur technique est survenue lors de l'envoi au serveur.
                    </p>
                    <div class="bg-gray-50 p-4 rounded-lg">
                        <p class="text-sm text-gray-500 mb-2">Pas de panique, vous pouvez nous écrire directement :</p>
                        <a href="mailto:contact@nashi.cloud" class="text-nashi-600 font-bold hover:underline">contact@nashi.cloud</a>
                    </div>
                    <button @click="step = 'form'" class="mt-4 text-sm text-gray-500 hover:text-gray-700 underline">
                        Réessayer
                    </button>
                </div>

            </div>
        </div>
    </div>
</div>